# LLM Profile Configuration
# Switch profiles by setting ACTIVE_PROFILE in .env or environment
# Each profile defines narrator, brain (triage/extraction/compaction), and memory settings
#
# Memory settings:
#   buffer_size: Messages held in buffer before overflow → compaction.
#     This IS the narrator's message window — the bookend layout uses all of them.
#     Larger = more verbatim history, but more tokens in the low-attention middle zone.
#   compaction_threshold: Overflow messages before triggering summary generation.
#   narrator_messages: Max recent messages injected into narrator context.
#     Defaults to buffer_size if not set. Allows decoupling buffer from context.

# Production: Sonnet narrates, Groq handles brain
# Sonnet handles 200K context — can use a large message window
production:
  narrator:
    provider: anthropic
    model: claude-sonnet-4-6
  brain:
    provider: groq
    model: qwen/qwen3-32b
    fallback_to_ollama: true
  memory:
    buffer_size: 40
    verbatim_size: 12
    condensed_size: 16
    compaction_threshold: 10

# Local MoE: everything on one model, zero cost
# Local 35B MoE: 32K context, weaker middle-attention — keep window moderate
local_moe:
  narrator:
    provider: ollama
    model: qwen3.5:35b-a3b
  brain:
    provider: ollama
    model: qwen3.5:35b-a3b
    fallback_to_ollama: false
  memory:
    buffer_size: 24
    verbatim_size: 8
    condensed_size: 10
    compaction_threshold: 6

# Local 9B: lightweight dense model for everything, fits any GPU
local_9b:
  narrator:
    provider: ollama
    model: qwen3.5:9b
  brain:
    provider: ollama
    model: qwen3.5:9b
    fallback_to_ollama: false
  memory:
    buffer_size: 20
    verbatim_size: 6
    condensed_size: 8
    compaction_threshold: 5

# Local split: MoE narrates, dense 27B does brain
local_split:
  narrator:
    provider: ollama
    model: qwen3.5:35b-a3b
  brain:
    provider: ollama
    model: qwen3.5:27b
    fallback_to_ollama: false
  memory:
    buffer_size: 24
    verbatim_size: 8
    condensed_size: 10
    compaction_threshold: 6

# Local dense: 27B does everything
local_dense:
  narrator:
    provider: ollama
    model: qwen3.5:27b
  brain:
    provider: ollama
    model: qwen3.5:27b
    fallback_to_ollama: false
  memory:
    buffer_size: 24
    verbatim_size: 8
    condensed_size: 10
    compaction_threshold: 6

# Groq only: cloud 32b does everything
groq_full:
  narrator:
    provider: groq
    model: qwen/qwen3-32b
  brain:
    provider: groq
    model: qwen/qwen3-32b
    fallback_to_ollama: false
  memory:
    buffer_size: 30
    verbatim_size: 10
    condensed_size: 12
    compaction_threshold: 8

# Groq Qwen3-32B narrates, local 9B does brain
groq_local:
  narrator:
    provider: groq
    model: qwen/qwen3-32b
  brain:
    provider: ollama
    model: qwen3.5:9b
    fallback_to_ollama: false
  memory:
    buffer_size: 30
    verbatim_size: 10
    condensed_size: 12
    compaction_threshold: 8

# MoE narrates locally, Groq does brain
moe_groq:
  narrator:
    provider: ollama
    model: qwen3.5:35b-a3b
  brain:
    provider: groq
    model: qwen/qwen3-32b
    fallback_to_ollama: false
  memory:
    buffer_size: 24
    verbatim_size: 8
    condensed_size: 10
    compaction_threshold: 6

# Qwen 3.6 Plus (free via OpenRouter) narrates, local 27B brain
# OpenRouter models handle large context well
qwen36_local:
  narrator:
    provider: openrouter
    model: qwen/qwen3.6-plus:free
  brain:
    provider: ollama
    model: qwen3.5:27b
    fallback_to_ollama: false
  memory:
    buffer_size: 30
    verbatim_size: 10
    condensed_size: 12
    compaction_threshold: 8

# Haiku narrator + local 27B brain (strong brain, needs dedicated VRAM)
haiku_27b:
  narrator:
    provider: anthropic
    model: claude-haiku-4-5-20251001
  brain:
    provider: ollama
    model: qwen3.5:27b
    fallback_to_ollama: false
  memory:
    buffer_size: 30
    verbatim_size: 10
    condensed_size: 12
    compaction_threshold: 8

# Haiku narrator + local 9B brain (fast, fits alongside other GPU workloads)
haiku_local:
  narrator:
    provider: anthropic
    model: claude-haiku-4-5-20251001
  brain:
    provider: ollama
    model: qwen3.5:9b
    fallback_to_ollama: false
  memory:
    buffer_size: 30
    verbatim_size: 10
    condensed_size: 12
    compaction_threshold: 8

# Sonnet narrator + local 27B brain
# Sonnet handles 200K context
sonnet_local:
  narrator:
    provider: anthropic
    model: claude-sonnet-4-6
  brain:
    provider: ollama
    model: qwen3.5:27b
    fallback_to_ollama: false
  memory:
    buffer_size: 40
    verbatim_size: 12
    condensed_size: 16
    compaction_threshold: 10
