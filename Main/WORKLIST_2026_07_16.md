# Verified worklist — 2026-07-16

Provenance: 33-agent survey over REFACTOR_PLAN.md, AUDIT_QUALITY_2026_06_09.md, AUDIT_FULL_2026_05.md, AUDIT_2026_05_28.md, AUDIT_DATAFLOW_2026_05_29.md, AUDIT_IMMERSION_2026_04.md, TODO.md, code TODO markers, and the 103 commits since 2026-06-09. 216 raw findings → 74 dropped as already fixed → 24 top candidates adversarially verified against the working tree → **22 confirmed still open** (each with file:line evidence, Appendix A). Suite at survey time: **750/750 green in 41.2s; mypy clean (133 files)**.

Original finding ids are kept (AQ-* = AUDIT_QUALITY_2026_06_09, DF-* = AUDIT_DATAFLOW_2026_05_29, C*/N*/R* = AUDIT_2026_05_28, TODO-* = TODO.md).

**Status 2026-07-16 (same day):** Do-now #1–#5 and all seven quick wins landed on branch `fix-sweep-2026-07-16` — 805 passed / 1 skipped, mypy clean. Checked boxes below are done; the "Next" tier and refactor-plan steps remain open.

## Do now

- [x] **1. Effect pipeline silently lies about state changes** (DF-12 + Step-1 leftovers) — three defects, same file, same failure mode (narration says X happened, state says it didn't or happened twice):
  - [x] Idempotency hits still re-apply: gate is `if result.success:` only; `was_duplicate=True` results still run `world_store.apply_effect` and `_last_executed_effects.append` (`dnd_bot/llm/orchestrator.py:2981-2992`). Fix is `and not result.was_duplicate`. **S**
  - [x] Drop-item never looks up `item_id`; returns success and narrates the drop while inventory is untouched (`orchestrator.py:1985`). **S**
  - [x] `requires_confirmation` effects are logged then `continue`d — dropped entirely, no UI queue exists (`orchestrator.py:2954`). Give it tool-path semantics or delete the knob with INTENTS. **S/M**
  - Deferred sibling (document only): narrator-path dedup is a volatile in-memory `_BoundedKeySet` (`orchestrator.py:512,603`, `effects.py:658/680`) while the tool path uses the durable `transaction_log` — divergent retry guarantees across restart now that ROOT-3 made resume real. **M**
- [x] **2. Streaming narration dies silently past ~2000 chars** (C23) — `_handle_narrative_token` truncates at 4000 but plain-message edits cap at 2000; the HTTPException hides in `except Exception: pass` (`dnd_bot/bot/frontends/discord_text.py:210-217`). Cap at ~1900 (trailing window) or switch preview to an embed; log the failure. Final chunking (:373-374) is already correct — only streaming is broken. **S**
- [x] **3. Stale scene state on location change** (DF-18) — `CHANGE_LOCATION` updates location/description but never clears `world_state.scene_items` or the scene registry roster (`dnd_bot/game/world_store.py:404-422`, `world_state.py:263-267`); stale items serialize into narrator YAML unconditionally (`world_state.py:583-587`); departed NPCs get `location=""` but stay alive, name-resolvable, combat-targetable (`world_state.py:371-376`, `orchestrator.py:3085/3111`). `registry.clear()` exists but is only called at session end. CAUTION: Stage C's roster-seed was reverted (63237bf) for flooding 4 consumers — clearing must be verified against the same consumers. **M**
- [x] **4. Concentration never clears on failed save** (DF-8, surviving leg) — failed CON save calls only `manager.break_concentration` (strips CombatEffects) but never clears `Character.concentration_spell_id` or the DB row (`dnd_bot/game/combat/coordinator.py:1287-1289`, `manager.py:1019-1040`; `sync_to_character` at `manager.py:734-777` omits concentration). Off-turn-check and cache-miss legs already fixed (d27a362). **S**
- [x] **5. JSON schemas not enforced on cloud providers** *(landed with a caveat: Gemini enforces real `response_schema` for extraction/memory schemas but falls back to mime-type-only for triage/state_delta — their free-form dicts are inexpressible as proto Schema; Anthropic uses a forced tool; Ollama plumbs `json_mode`/`json_schema`/`think` through the compat path)* (R4) — Anthropic gets only a "respond with ONLY JSON" hint (`dnd_bot/llm/client.py:1126-1132`); Gemini sets `response_mime_type` but never `response_schema` (:1389-1390); Ollama tool-bearing calls route through `_chat_via_openai_compat` which silently drops `json_mode`/`json_schema`/`think` (:221-233, :495-506); Groq/DeepSeek/OpenRouter downgrade schema to bare json_object. Triage/extractor reliability rests on `extract_json_object` salvage (6 importers). Upstream of every "extractor missed it" symptom. **M**

## Quick wins (S-effort, besides the S items above)

- [x] Fake "Failed to create character" after successful creation: logger reads nonexistent `character.race`/`class_name` (model has `race_index`/`class_index`) — `dnd_bot/bot/cogs/character.py:517-518`. Two attribute renames. (N7)
- [x] Command spell applies Prone for all 6 variants, not just Grovel — `coordinator.py:78`, consumed at :1127-1128. (C11)
- [x] Adv/dis silently dropped for non-1d20 rolls + vacuous nat-20/nat-1 tests (`if nat_20_found: assert True`) with no RNG seeding anywhere in tests — `game/mechanics/dice.py:115`, `tests/unit/test_dice.py:93-94/106-107`. One slice fixes both. (C13)
- [x] Voice-catalog seeding permanently disabled by any mid-seed crash: `_catalog_seeded=True` set before the work runs; count-based gate + INSERT OR IGNORE make renames/removals invisible; log reports JSON length not inserts — `data/repositories/immersion_repo.py:92-115,170,182`. (C3)
- [x] Executor leak + wedgeable pool: Ollama timeout abandons threads (no HTTP timeout on the client, `client.py:169,290-294`), 4 stuck calls wedge `max_workers=4` forever; `close()` has zero callers and `_reset_clients` leaks an executor per `/profile` switch (`client.py:832-834,1806-1812`, `config.py:189-190`). (AQ-ASYNC-08)
- [x] Narrator scratchpad leaks across sessions: `scratchpad_clear` (`orchestrator.py:736`) has zero production callers; `set_session` (:629-632) never resets it, so session B's narrator gets session A's tensions/moods. (DF-22)
- [x] Unarmed-strike `damage_dice='1'`: every unarmed hit errors after consuming the action; two coordinator sites, pinned-broken tests waiting to flip in `test_combat_round.py`. (Step 3 net find, never landed)

## Next — verified P1s by subsystem

**Combat**
- [ ] Legacy `/combat` path drift (AQ-ARCH-04): attack adjudicates vs raw `armor_class` ignoring cover/dodge (`dnd_bot/bot/cogs/combat.py:633` vs `coordinator.py:539`/`manager.py:880-885`); `_sync_player_characters` called at only 3 of ~9 mutating subcommands; turn-end sync runs before `end_turn` ongoing damage (`combat.py:1573-1574`). Loss window is now mid-combat-crash only (47a4457 fixed combat-end persistence). Pairs with the bot-layer turn-loop consolidation. **M**
- [ ] Action economy burned on mid-handler crash (AQ-ERR-03): resources consumed before routing (`coordinator.py:425-447`), catch-all returns failure with no refund (:450-483); `_execute_spell` expends+persists the slot before target validation (:1017-1042) — a "failed" cast burns the slot and a retry double-applies. **M**
- [ ] Combat registry still channel-keyed (AQ-STATE-05): all voice/web combats land under `discord:0` (`encounter.py:309` + `voice/api.py:420/486/559`); racy check-then-set at `cogs/combat.py:105-120` while `try_set_combat_for_channel` (`manager.py:1317`) has zero callers; `end_session`/`leave_session` channel-only; `f"discord:{channel_id}"` at 9+ sites; `/combat` hardcodes `session_id="default"` (`cogs/combat.py:115`). **M**
- [ ] In-combat attacks fall through the attack-action branch — only encounter-start is handled (`orchestrator.py:972` TODO). **M**

**World state / extraction**
- [ ] No player-prose fallback extractor (TODO-03): StateDelta has zero player fields (`game/world_state.py:134-152`), extractor prompt covers no player axes (`extractors/state_extractor.py:28-49`) — if the narrator forgets `update_player`, HP/conditions/items/currency silently drift. NPC half fixed in d27a362; this player half is the design's biggest remaining hole. Real design work. **L**
- [x] `established_facts` append-only, uncapped, O(n²) dedup, duplicated across two stores into every prompt (DF-15). **Re-verified closed 2026-07-24** by the intervening fact-supersession work: contradicted facts are retired with provenance (`game/fact_supersession.py`, `world_store._supersede_conflicting_facts`), live prompts carry only the scene-anchored ≤20 projection (`world_state.get_scene_relevant_facts`; memory's `<established_facts>` block renders only for empty-input diagnostic views), and the per-turn sync respects retirement (`world_store.add_established_fact`). The raw ledger stays append-only by design — it is the complete campaign record; at realistic soak scale (~hundreds of facts) the O(n) scans are negligible. **M**

**LLM layer**
- [ ] Four copy-pasted OpenAI-compat provider bodies with drift, key-fallback on only 2 of 5 providers, two Gemini SDKs (AQ-DUP-05): `client.py:495-669/863-1000/1509-1607/1654-1790`; production still imports deprecated `google-generativeai` (`client.py:1259`, `pyproject.toml:19`). Not covered by the refactor plan. **L**
- [x] Synchronous ChromaDB embedding on the event loop each turn + redundant re-embeds + per-turn matcher rebuilds (AQ-ASYNC-03). **Closed 2026-07-24**: the per-turn call sites were already off-loop (`asyncio.to_thread` at `orchestrator.py:1817/1869/2131/2161/2279/2294`, verified); the last on-loop site — the session-start entity re-sync (`session.py:365-390`) — now runs in a worker thread; and `VectorStore.add_entity_description` gained a content-hash skip-unchanged gate (hash over the exact indexed document, stored in Chroma metadata, cache seeded once per campaign) so per-turn graph-op re-syncs and restart full re-syncs no longer re-embed unchanged entities. The `EntityNameMatcher` double-build was examined and deliberately left: the second build runs after the turn's graph ops and must see the updated graph (reusing the first index would miss this turn's new NPCs), and construction is O(N log N) pure CPU — microseconds at realistic rosters. **M**

**Data layer**
- [ ] No transactionality + every declared lock is dead (AQ-STATE-08): `character_repo.update()` runs 10+ statements incl. DELETE-then-reinsert with one trailing commit (`character_repo.py:374-485`); `Database._lock` never acquired (`database.py:25`); `_creation_states_lock` zero uses while the dict is mutated at 6 sites; repo singletons ignore injected Database (`character_repo.py:667-672`). **M**

**Bot cogs / UX**
- [ ] Campaign resolution pasted 13x with 3 divergent policies + 9-step creation wizard duplicated ~380 lines (AQ-DUP-02): paste block in `spells.py/rest.py/actions.py/combat.py`; first-campaign fallback divergence in `character.py:70-72`/`game.py:52-54`; wizard at `character.py:139-527` vs `campaign.py:332-767` with drifted finalize/membership. **L**
- [ ] `/immersion` commands write DB rows nothing reads — frontend builds settings purely from the profile (C21): `discord_text.py:219-251`, `cogs/immersion.py:27-121`; fix with the stale `'inworld'/'Diego'` migration defaults (`migrations/004_immersion_features.sql:11-12`). **M**
- [ ] Add durable class-feature resources before full rest recovery. The false
  `"All class features"` result and unknown-field assignments are fixed, but
  Character/DB still has no persisted feature counters. See
  `LONGFORM_READINESS_2026_07.md`. **M**
- [ ] Sell action is a stub returning canned refusal (`orchestrator.py:1880`). **S/M**

**Web/voice** (P0 label, but ranked here for a solo dev unless/until hosted)
- [ ] No per-client identity, zero auth, `allow_origins=["*"]`, hardcoded `dnd-session` room, module-global voice session, private `_sessions` dict surgery with no reconcile, `endGame()` sends no session key, identity is `hash(player_name)` (C2): `voice/api.py:42-47/62/115/326/429-430/662/671-678`, `web/app.js:610-612`. Do not expose beyond localhost until fixed. ROOT-3 explicitly left this lifecycle unowned. **M**

## Refactor plan

Done through Stage C (Steps 0/1a/1/2/3/4-core/5, ROOT-3, Stage C) at 750 green. Remaining, in the doc's own order:

| Step | Size | Notes |
|---|---|---|
| **Step 6 — thin coordinator + middleware spine (absorbs Stage B)** | L | The plan's own next entry. Stage B (per-turn TurnContext + per-session lock) is a component of it, not a parallel track — Step 4's deferral note routes it here. Orchestrator still ~3.2k non-blank lines. Fresh branch, pin-first. |
| Step 7 — delete strangled host + temp seams | M | Only after Step 6. AST-span-by-name; "dead" code here hides live helpers. |
| Bot-layer turn-loop consolidation (audit #87) | M | Needs a GameFrontend-shaped seam + netting strategy for venv-only cogs. Would absorb AQ-ARCH-04. |
| Cross-session WorldState re-convergence | S/M | Reverted in Stage C (63237bf); needs consumer-side location gating + its own adversarial review. DF-18's fix is a natural companion. |
| WorldStateView / reader migration | S/M | Conditional — "ceremony without a threat model" per the doc. |
| Step 1 leftovers (ProposedEffect union, CONSUME_RESOURCE, typed item-use effects, requires_confirmation) | S/M | Equip/unequip now persist; generic use remains blocked on item effect/charge metadata. The `requires_confirmation` piece overlaps Do-now #1. |
| INTENTS confirm-then-delete (~650 lines) + pre-net cleanup residue | S | Was gated on "until the net exists"; the net exists. Grep-confirmed deletions under 750 tests. |
| ROOT-3 non-goals (mid-combat resume, voice/web recovery, memory-tier persistence) | M each | Feature slices, not refactoring. Voice/web recovery pairs with C2. |

**Pick: Step 6**, after a low-risk deck-clearing session on the Do-now cluster + quick wins.

## Verified fixed — no action

- Stored XSS in the web UI (C1) — empirically false; `esc_attr`'s `&quot;` conversion keeps all four tested payloads inert in a real browser.
- Lobby/character-select authz gap (N4) — closed: DM-gated Start button (`views/campaign_lobby.py:92-97`), ephemeral character select (`cogs/campaign.py:140`).
- Partial fixes folded into open items above: DF-8's off-turn/cache-miss legs, TODO-03's NPC half (d27a362), DF-21's combat-end persistence loss (47a4457).

---

## Appendix A — verification evidence (as of 2026-07-16)

**AUDIT_2026_05_28/C2 (P0, M)** — Web/voice: no per-client identity, zero auth, wide-open CORS, no legitimate teardown. Evidence: `voice/api.py:42-47` `allow_origins=["*"]` etc.; zero FastAPI Depends/auth in the file; LiveKit endpoint (:668-699) unauthenticated with `devkey` fallback (:678); hardcoded room `dnd-session` (:62, :671); module-global `_active_voice_session` (:115, set :432, nulled :664); private-dict surgery `session_manager._sessions.pop/[...]` (:429-430, :662) with no end_session/persist/reconcile; `web/app.js:610-612` endGame sends no session key; identity `hash(req.player_name) & 0x7FFFFFFF` (:326, 483, 536). Refutation attempted: `git log -S"allow_origins"` shows only the introducing commit; post-June commits are logging/dead-code only.

**AUDIT_DATAFLOW_2026_05_29/DF-8 (P0, S)** — Concentration surviving leg. `coordinator.py:1287-1289` failed CON save calls only `manager.break_concentration`; `manager.py:1019-1040` removes CombatEffects only, never clears `Character.concentration_spell_id` or DB (`repo.update_concentration`'s sole product caller is `coordinator.py:1188`, set-on-cast only; `sync_to_character` `manager.py:734-777` omits concentration). Fixed legs: off-turn check (`coordinator.py:1251-1257` session-first via `_resolve_player_character` :1295-1309, d27a362); cache-miss spell failure (`coordinator.py:980` awaits `_get_character` with repo fallback :1311-1323).

**AUDIT_2026_05_28/C3 (P0, S)** — Voice-catalog seeding. `immersion_repo.py:92-94` flag set BEFORE seed work (:96-115) so mid-seed exception disables seeding for the process; :107 count-based gate hides renames/removals; :170 INSERT OR IGNORE never updates; :182 + :113-114 log reports JSON length not inserts. Only commits since June (6d3ad93, d27a362) were typing changes.

**AUDIT_QUALITY_2026_06_09/AQ-ARCH-04 (P1, M)** — Legacy /combat drift. `cogs/combat.py:633` adjudicates vs raw `armor_class` while `coordinator.py:539` uses `manager.get_effective_ac()` (cover at `manager.py:880-885`); `_sync_player_characters` at only 3 sites (:748 damage, :819 heal, :1573 turn-end) — attack/opportunity/death_save/stabilize/temp_hp/set_hp/next mutate with no sync; :1573-1574 syncs BEFORE `end_turn` ongoing damage. Mitigation: 47a4457 (P0-3) single teardown owner calls `persist_player_characters()` on every combat-over path (`session.py:1266`); remaining loss window is mid-combat process death.

**AUDIT_QUALITY_2026_06_09/AQ-STATE-05 (P1, M)** — Channel-keyed combat registry. `encounter.py:309` `set_combat_for_channel(session.channel_id, ...)` with `voice/api.py:420/486/559` setting `channel_id=0`; `cogs/combat.py:105-120` racy get-check-set; `manager.py:1317` `try_set_combat_for_channel` zero callers; `set_combat_by_key` zero production callers; `session.py:722` `end_session` and :871 `leave_session` channel-only; `f"discord:{channel_id}"` at `session.py:115/274/724/735/1246`, `campaign.py:868/909`, `manager.py:1294`, `coordinator.py:1788`.

**AUDIT_2026_05_28/R4 (P1, M)** — Schemas not enforced. `client.py:1126-1132` Anthropic hint-only; :1389-1390 Gemini no `response_schema`; :221-233 + :495-506 Ollama tool path drops `json_mode`/`json_schema`/`think` (docstring :152-153 admits it); :905-906/:1710-1711/:1548-1549 Groq/DeepSeek/OpenRouter downgrade to bare json_object. Callers depending on it: `orchestrator.py:1515`, `extractors/entity_extractor.py:171`, `extractors/state_extractor.py:154`, `memory/manager.py:569`; 6 modules import `extract_json_object` salvage. `git log -S"response_schema"` on client.py is empty.

**AUDIT_QUALITY_2026_06_09/AQ-STATE-08 (P1, M)** — No transactions, dead locks. `character_repo.py:374-485` update() 10+ statements (DELETE condition :421 then re-INSERT :426-441; spell DELETE/INSERT/UPDATE :459-481) with single trailing commit :483, no `db.transaction()` (only create() :57 has one). `database.py:25` `self._lock` never acquired; `campaign.py:50` `_creation_states_lock` zero async-with while `_creation_states` mutated at `campaign.py:332,373,642,719`, `character.py:139,409,485`; `campaign.py:53-61` `_add_campaign_player` sole lock user, no callers; `character_repo.py:667-672` singleton ignores injected Database.

**AUDIT_QUALITY_2026_06_09/AQ-ASYNC-03 (P1, M)** — Sync ChromaDB on event loop. `memory/vector_store.py:38-59` lazy PersistentClient all sync; :86/:120/:172/:355 sync add/update/query/upsert; `orchestrator.py:1106,1131,1264,1289,1397,1411` sync calls inside async `process_action`, no `to_thread` (run_in_executor exists only in llm/client.py, voice/, immersion/); :1256-1271 no description-hash gate; `session.py:360-373` O(N) full KG→Chroma upsert on every start_session AND recover_sessions; `orchestrator.py:1092`/:1281 + `matcher.py:28,45-46` EntityNameMatcher built twice per turn.

**AUDIT_DATAFLOW_2026_05_29/DF-22 (P1, S)** — Scratchpad leak. `orchestrator.py:610-612` `_scratchpad` on singleton; :629-632 `set_session` no reset; :736-739 `scratchpad_clear` sole caller is `tests/unit/test_memory.py:474`; :822-825 scratchpad injected into narrator session_summary; :3249-3258 process-wide singleton; `_reset_orchestrator` (:3261) only called on profile switch (`config.py:198-199`).

**AUDIT_DATAFLOW_2026_05_29/DF-15 (P1, M)** — established_facts. Append-only O(n²) dedup no cap/retraction: `world_state.py:387-390`, `world_store.py:107-108`, `memory/blocks.py:228-229` (docstring :240 "must survive indefinitely"); serialized into every prompt: `world_state.py:602-603` + `memory/manager.py:679-683`; per-turn one-way pinned→WS copy `session.py:1018-1022` (narrator sees facts twice); 716ce3e capped other blocks, explicitly deferred this (`manager.py:672-678` comment).

**TODO/AUDIT_FULL_2026_05/TODO-03 (P1, L)** — Player-prose extractor hole. `world_state.py:134-152` StateDelta zero player fields; `state_extractor.py:28-49` prompt has no player axes; only mutation paths are narrator-declared (`tool_registry.py:474` update_player, executed `effects.py:1171`; legacy `intents.py`). NPC half FIXED since d27a362 (NPCUpdate carries add/remove_inventory, important, alive, notes — `world_state.py:111-122`, prompt `state_extractor.py:39-43`, applied `world_state.py:333-334`, synced incl. extractor-channel death `orchestrator.py:2800-2807`).

**AUDIT_DATAFLOW_2026_05_29/DF-18 (P1, M)** — Stale scene state. `world_store.py:404-422` CHANGE_LOCATION never clears `scene_items` nor registry; `world_state.py:263-267` extractor path likewise; :583-587 stale items serialize unconditionally; :371-376 removed NPCs get `location=""` only (stay in `self.npcs`, alive, resolvable via `_find_npc`; npc_updates :290-317 can re-set location — only dead NPCs blocked :299-303); `registry.py:200-206` clear() only invoked at session end (`session.py:752`); `orchestrator.py:3085/3111` combat targeting iterates full registry, no location gate. No fix commits (`-S"scene_items"`/`-S"CHANGE_LOCATION"` — only code moves ada5644, 9b6777e, 874c467).

**AUDIT_QUALITY_2026_06_09/AQ-DUP-05 (P1, L)** — Provider duplication. Four drifted OpenAI-compat bodies: `client.py:495-669` (Ollama compat), :863-1000 (Groq), :1509-1607 (OpenRouter), :1654-1790 (DeepSeek) — Groq cleans content :952, OpenRouter :1595 and DeepSeek :1773 return raw; dotenv empty-string-shadow fallback only Anthropic :1069-1073 / Gemini :1270-1273; two Gemini SDKs (`pyproject.toml:19` legacy vs :47 google-genai; production import of deprecated SDK `client.py:1259`; deprecation warning in `data/long_horizon_run.log:5-6`).

**AUDIT_QUALITY_2026_06_09/AQ-ASYNC-08 (P1, S)** — Executor leak. `client.py:290-294` wait_for(run_in_executor) cancel abandons thread blocked in `ollama.Client.chat` (client built :169 no HTTP timeout); :160/:168 max_workers=4; same pattern :720-723, :736, :804-807, :830; `close()` :832-834 zero call sites; `_reset_clients` :1806-1812 nulls references only, invoked per /profile switch (`config.py:189-190`) — leaks an executor each time.

**AUDIT_DATAFLOW_2026_05_29/DF-12 (P1, S)** — Duplicate effects re-applied. `orchestrator.py:2981-2992` gate `if result.success:` only; `was_duplicate` logged (:2986) but `world_store.apply_effect(effect)` (:2991) and `_last_executed_effects.append` (:2992) still run; `world_store.py:323` apply_effect has no internal idempotency. Divergence half: `effects.py:658/680` dedups via volatile `_BoundedKeySet(maxlen=1000)` (`orchestrator.py:512,603`) vs durable SQLite transaction_log on the tool path (`orchestrator.py:3197-3202`, `transaction_repo.py`).

**AUDIT_2026_05_28/C23 (P1, S)** — Streaming narration cap. `discord_text.py:210-217` `display = text[:4000]...` then plain `send`/`edit` (2000-char cap) inside `except Exception: pass  # Best effort streaming`; once streamed text exceeds ~1998 chars every edit raises HTTP 400, swallowed, streaming dead for the turn. `-S"4000"` shows only c96bbf8; the exc_info sweep (39ce838) didn't touch this block. Final narrative separately chunked correctly (:373-374, `_split_text(EMBED_DESCRIPTION_LIMIT - 100)`).

**AUDIT_2026_05_28/C21 (P1, M)** — /immersion no-op. `discord_text.py:219-251` `_get_immersion_settings` builds solely from `get_profile().immersion` (no `get_immersion_repo()` anywhere in file); :393-407 gating uses profile-only settings; `cogs/immersion.py:27-121` all five commands upsert rows nothing reads; `migrations/004_immersion_features.sql:11-12` stale defaults `'inworld'/'Diego'` vs `'kokoro'/'af_heart'` (`models/immersion.py:23-24`, `config.py:51-52`).

**AUDIT_QUALITY_2026_06_09/AQ-ERR-03 (P1, M)** — Action economy on crash. `coordinator.py:425-447` consumes action/bonus/reaction before routing; :450-483 catch-all returns failure, no refund/rollback; :1017-1024 `_execute_spell` expends + persists slot (repo.update_spell_slot) before target validation (:1037-1042) and before apply_damage (:1062); `manager.py:513-532` no restore path (reset only at turn start); no refund/restore code anywhere combat-related.

**AUDIT_QUALITY_2026_06_09/AQ-DUP-02 (P1, L)** — Cog duplication. Paste block at 13 sites (`spells.py:367,506,594,655,713,763`; `rest.py:52,157,251,393`; `actions.py:172,278`; `combat.py:179` — all carry the literal comment "Resolve the active campaign from the guild's lobby state (audit #52)"); divergent helpers `inventory.py:32` (session-first) vs `character.py:54`/`game.py:38` (first-campaign fallback :70-72/:52-54); wizard `character.py:139-527` vs `campaign.py:332-767` (proficiency_choices verbatim at `character.py:302` vs `campaign.py:537`); `campaign.py:722-725` `_finalize_character` registers `_campaign_players` while character.py's finalize does not.

**AUDIT_2026_05_28/N7 (P2, S)** — Fake creation failure. `character.py:517-518` logs `race=character.race, class_name=character.class_name` after the success followup (:510); Character model (`models/character.py:263`) defines only `race_index`/`class_index` → AttributeError caught :521, :524 sends "Failed to create character".

**AUDIT_2026_05_28/C11 (P2, S)** — Command spell. `coordinator.py:78` `"command": (Condition.PRONE, 1, False),  # "grovel" variant` consumed :1127-1128 keyed only on spell_index, no variant handling.

**AUDIT_2026_05_28/C13 (P2, S)** — Dice adv/dis. `dice.py:115` `if (advantage or disadvantage) and num_dice == 1 and die_size == 20:` — any other notation silently rolls normal. `test_dice.py:93-94` `if nat_20_found: assert True` (vacuous; same :106-107); zero RNG seeding in Main/tests.

**AUDIT_2026_05_28/C2 → see top of appendix; AQ-* remaining items above carry their evidence inline.**
